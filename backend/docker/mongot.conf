syncSource:
  replicaSet:
    hostAndPort: "mongod.mongo-network:27017"
    username: mongotUser
    passwordFile: /mongot-community/pwfile
    authSource: admin
    tls: false
    readPreference: primaryPreferred
storage:
  dataPath: "data/mongot"
server:
  grpc:
    address: "mongot.mongo-network:27028"
    tls:
      mode: "disabled"
metrics:
  enabled: true
  address: "mongot.mongo-network:9946"
healthCheck:
  address: "mongot.mongo-network:8080"
logging:
  verbosity: INFO
